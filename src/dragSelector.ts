
interface SelectorOptions {
    // TODO
}

// TODO
class DragSelector {

    constructor(opts?: SelectorOptions) {

    }

}

export default function(opts?: SelectorOptions) {
    return new DragSelector(opts);
}